# F1 data API — no auth required, publicly readable, CORS open (Access-Control-Allow-Origin: *).
# f1connectapi.vercel.app 307-redirects here; using the resolved host avoids a cross-origin redirect hop.
VITE_F1_API_BASE=https://f1api.dev/api
