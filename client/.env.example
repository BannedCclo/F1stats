# Local Express API (server/), started via `npm run dev` (server) or
# `npm run dev:all` (server), which listens on port 3000 by default.
VITE_F1_API_BASE=http://localhost:3000/api

# To point at the public hosted API instead, use:
# VITE_F1_API_BASE=https://f1api.dev/api
