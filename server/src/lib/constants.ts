export const SITE_URL = "https://f1api.dev"
export const SITE_NAME = "F1 API"
// "Current season" moved to lib/currentYear.ts's getCurrentYear() — derived
// from the database instead of a hardcoded literal, so it doesn't need a
// code change every January.
